## Sintesi

Abbiamo utilizzato ChatGPT e Claude come strumenti di supporto allo studio, alla progettazione e alla revisione del codice.

Gli assistenti sono stati configurati per lavorare principalmente come tutor: porre domande, spiegare i concetti, indicare problemi e proporre piccoli passi, senza fornire immediatamente la soluzione completa.

Il codice del componente `DNSLookup` è stato scritto e modificato progressivamente da noi, verificando manualmente il comportamento dopo ogni modifica.

In una sessione successiva abbiamo usato l'IA in modo diverso: non solo per il codice, ma anche per **riallineare tutta la documentazione** (`README.md`, `manuale-utente.md`, `manuale-tecnico.md`, `scelte.md`, `CLAUDE.md`, `conftest.py`) allo stato reale del progetto dopo l'aggiunta di `IpCalculator`. In questo caso il testo dei manuali è stato scritto in gran parte direttamente dall'IA, non riformulato da noi: è una differenza onesta da segnalare rispetto al codice, dove abbiamo sempre scritto noi le righe (anche quando piccole, come l'aggiornamento di `__init__.py`).

## Dettaglio per parte

| Parte del progetto            | Cosa abbiamo chiesto                                                                   | Cosa abbiamo accettato, modificato o rifiutato                                          | Perché                                                                         |
|-------------------------------|----------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
| Struttura di `DNSLookup`      | Chiarimenti sull’ereditarietà da `Tool` e sul ruolo di `execute(target)`               | Abbiamo adottato `DNSLookup(Tool)` e mantenuto il dominio come parametro di `execute()` | Per rispettare il contratto polimorfico comune agli strumenti                  |
| Import relativi               | Spiegazione della sintassi `from .tool import Tool`                                    | Abbiamo adottato l’import relativo                                                      | I moduli appartengono allo stesso package `project`                            |
| Libreria DNS                  | Indicazioni sul modulo da utilizzare con `dnspython`                                   | Abbiamo utilizzato `dns.resolver` e `dns.exception`                                     | Offrono query DNS ed eccezioni specifiche                                      |
| Supporto multi-record         | Discussione su come aggiungere `AAAA`, `MX` e `TXT`                                    | Abbiamo trasformato il singolo tipo di record in una lista di tipi                      | Consente una o più interrogazioni con la stessa esecuzione                     |
| Validazione della CLI         | Supporto per interpretare `A,MX`, spazi, input non validi e `ALL`                      | Abbiamo implementato personalmente parsing e controlli nel main                         | La classe rimane indipendente dall’interazione con l’utente                    |
| File `__main__.py` temporaneo | Richiesta esplicita di una versione pronta per testare DNSLookup e PortScanner         | Abbiamo accettato una struttura iniziale del menu, riservandoci di modificarla          | Serviva a eseguire subito test integrati senza deviare dal lavoro sul DNSLookup|
| Test manuali                  | Richiesta di casi utili per verificare successo, record mancanti e dominio inesistente | Abbiamo eseguito personalmente i test e controllato gli output                          | Per confermare la correttezza del comportamento reale                          |
| Aggiunta automatica di .json  | Richiesta su come aggiungere automaticamente .json al percorso specificato             | Abbiamo accettato e applicato quanto consigliato                                        | Perchè un utente potrebbe scordare l'estensione del file                       |
| Algoritmo VLSM di `IpCalculator` | Spiegazioni sulla tecnica VLSM, su come calcolare il numero minimo di bit host per una sottorete e sull'ordine in cui allocare le sottoreti | Abbiamo adottato l'ordinamento decrescente degli host richiesti e la formula `math.ceil(math.log2(host + 2))`, ma abbiamo scritto e testato noi l'implementazione con il modulo `ipaddress` | Partire dai requisiti più grandi evita di frammentare lo spazio di indirizzi disponibile |
| Gestione errori in `IpCalculator` | Confronto tra intercettare l'errore sugli host richiesti separatamente o nello stesso blocco `try` della validazione dell'indirizzo | Abbiamo mantenuto entrambi i controlli nello stesso blocco `except ValueError`, documentando il comportamento (messaggio con prefisso fisso) invece di correggerlo | Per restare coerenti con il tempo a disposizione in questa fase del progetto, rimandando un eventuale miglioramento |
| `src/project/__init__.py` incompleto | Richiesta di controllare tutti i file sotto `src/project/` per capire cosa mancasse | L'IA ha segnalato che `__init__.py` esponeva solo `Tool` e `PortScanner`, dimenticando `DNSLookup`, `IpCalculator` ed `export_report`; le righe di import mancanti le abbiamo scritte noi, seguendo il metodo tutor | Il file doveva restare coerente come "import ponte" del package, come richiesto dal nostro stesso `CLAUDE.md` |
| Design di `report_exporter.py` (funzione vs classe) | Discussione guidata (domande, non risposte dirette) su cosa distingue una funzione da una classe in OOP e sul flusso dei dati tra `Tool.execute()` ed `export_report` | Abbiamo ragionato e risposto noi alle domande; abbiamo poi chiesto esplicitamente all'IA di scrivere il paragrafo di sintesi in `scelte.md`, che quindi è testo dell'IA e non nostro | Il ragionamento (criterio stato/comportamento, basso accoppiamento) l'abbiamo capito e prodotto noi; abbiamo delegato solo la trascrizione in prosa del paragrafo di documentazione |
| Aggiornamento completo della documentazione (README, manuali, `scelte.md`, `CLAUDE.md`, `conftest.py`) | Richiesta esplicita di rileggere tutti i documenti e segnalare cosa mancasse o non fosse più coerente col codice, poi di scrivere le correzioni | Abbiamo accettato le correzioni meccaniche (nomi di file, comandi, refusi di template tipo `progetto`/`core`); su `docs/proposta.md` abbiamo deciso noi se ripristinare la versione storica o tenere quella riscritta a consuntivo | La scelta su cosa tenere del punto di partenza storico del progetto doveva restare nostra, non dell'IA, per non falsificare la cronologia |

## Cosa non abbiamo delegato all’IA

Non abbiamo delegato all’IA la comprensione finale del codice, la scrittura del codice sorgente (comprese le modifiche piccole, come `__init__.py`), l’esecuzione dei test, la verifica degli output, la scelta definitiva delle modifiche da conservare (incluso cosa ripristinare in `docs/proposta.md`) e la gestione della repository Git.

Abbiamo invece delegato la stesura letterale della maggior parte del testo dei documenti in `docs/` e del `README.md`, oltre a un paragrafo di `scelte.md` (`report_exporter.py`): il ragionamento dietro quel paragrafo è nostro, ma le frasi sono state scritte dall'IA e accettate senza riformularle a mano.

Le entry del devlog e le riflessioni personali sul lavoro del gruppo non sono interamente scritte da noi, per una questione di tempi: le prime settimane non sono state sfruttate al massimo date le mille verifiche, nell'ultima settimana abbiamo dedicato tanto tempo al progetto ma non siamo riusciti anche a scrivere tutte le documentazioni a mano
